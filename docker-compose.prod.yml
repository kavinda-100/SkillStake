# Production environment overrides
services:
    backend:
        build:
            target: production
        restart: always
        environment:
            NODE_ENV: production

    frontend:
        build:
            target: production
        restart: always
        environment:
            NODE_ENV: production
